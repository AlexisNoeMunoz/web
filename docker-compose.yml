version: '3.3'

services:
    ruby:        
        build: .       
        image: alexisnoe27/alexis_app:master  
        command: bash -c 'bundle install && bundle update && rake start'
        stdin_open: true
        tty: true
        ports: 
            - 3000:3000        
        volumes: 
            - ./:/rails_app

#docker exec -it name /bin/bash